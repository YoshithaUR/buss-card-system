export const registerController = async (request, response) => {

    const { name, email, password, address, age, school, district, hometown, gender, image, startLocation, endLocation, category } = request.body
    console.log('running')

}